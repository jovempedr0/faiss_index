# Environments
.venv/
.env

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/
.pytest_cache/

# OS
.DS_Store

# Local test fixtures (real documents) and generated indices — kept on disk, not versioned
fixtures/casos/
fixtures/faiss_index_output/

# Claude
.claude/

# mkdocs build output (the site is published by .github/workflows/docs.yml)
site/

# Local-only evaluation data and the pre-GLM baseline index (real case documents).
# The scripts in fixtures/eval/ are versioned; what they read and write is not.
fixtures/eval/*.json
fixtures/faiss_index_output_tesseract/
